<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Assets/Colors.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- Общие ресурсы -->
    <SolidColorBrush x:Key="WhiteButtonBackground" Color="White"/>
    <Thickness x:Key="ButtonPadding">20,10</Thickness>
    <CornerRadius x:Key="ButtonCornerRadius">24</CornerRadius>

    <!-- Базовый шаблон кнопки -->
    <ControlTemplate x:Key="BaseButtonTemplate" TargetType="Button">
        <Border x:Name="Border"
                CornerRadius="{StaticResource ButtonCornerRadius}"
                Background="{TemplateBinding Background}"
                >
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="TranspButtonTemplate" TargetType="Button" >
        <Border x:Name="Border"
                CornerRadius="{StaticResource ButtonCornerRadius}"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                SnapsToDevicePixels="True"
                UseLayoutRounding="True">
            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
        </Border>
    </ControlTemplate>
    

    <!-- Белая кнопка -->
    <Style x:Key="white_btn" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource WhiteButtonBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource ButtonBrush_Green}"/>
        <Setter Property="FontSize" Value="36"/>
        <Setter Property="FontFamily" Value="SB Sans Text"/>
        <Setter Property="Padding" Value="{StaticResource ButtonPadding}"/>
        <Setter Property="BorderBrush" Value="White"/>
        <Setter Property="BorderThickness" Value="5"/>
        <Setter Property="Template" Value="{StaticResource BaseButtonTemplate}"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource HyperLink_Yellow}"/>
                <Setter Property="BorderBrush" Value="{StaticResource HyperLink_Yellow}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#f0fff0"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Прозрачная кнопка -->
    <Style x:Key="transparent_btn" TargetType="Button" BasedOn="{StaticResource white_btn}">
        <Setter Property="Template" Value="{StaticResource TranspButtonTemplate}"/>
        <Setter Property="Background" Value="{StaticResource ButtonBrush_transparent}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="White"/>
        <Setter Property="BorderThickness" Value="2"/>
        
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#50FFFFFF"/>
                <Setter Property="BorderThickness" Value="4"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Кнопка для попапа -->
    <Style x:Key="popup_btn" TargetType="Button" BasedOn="{StaticResource white_btn}">
        <Setter Property="Background" Value="{StaticResource ButtonBrush_Green}"/>
        <Setter Property="Foreground" Value="White"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#38997B"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="back_btn" TargetType="Button" BasedOn="{StaticResource transparent_btn}">
        <Setter Property="Width" Value="223"/>
        <Setter Property="Height" Value="62"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border CornerRadius="12"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
