<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Poll_ver2.Converters">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Assets/Colors.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="RichTextBox" TargetType="RichTextBox">
        <Setter Property="FontFamily" Value="{StaticResource TextFont}" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="FontSize" Value="18" />
    </Style>

    <!--  Конвертер для расчета ширины прогресса  -->
    <local:SliderConverter x:Key="SliderConverter" />

    <!--  Стиль для ползунка (Thumb)  -->
    <Style x:Key="SliderThumbStyle" TargetType="{x:Type Thumb}">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Height" Value="71" />
        <Setter Property="Width" Value="71" />
        <Setter Property="Margin" Value="0,10,0,0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Ellipse x:Name="Ellipse" StrokeThickness="1">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="/Assets/Resourses/slider.png" />
                        </Ellipse.Fill>
                    </Ellipse>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Основной шаблон для горизонтального Slider  -->
    <ControlTemplate x:Key="HorizontalSlider" TargetType="{x:Type Slider}">
        <Grid ClipToBounds="False">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Фоновая дорожка  -->
            <Border
                x:Name="TrackBackground"
                Grid.Row="1"
                Height="37"
                Margin="25,0,25,0"
                Background="{StaticResource ButtonBrush_transparent}"
                BorderThickness="1"
                CornerRadius="20" />

            <!--  Полоса прогресса (заполняющаяся часть)  -->
            <Rectangle
                x:Name="ProgressBar"
                Grid.Row="1"
                Height="37"
                Margin="25,0,25,0"
                HorizontalAlignment="Left"
                Fill="White"
                RadiusX="20"
                RadiusY="20">
                <Rectangle.Width>
                    <MultiBinding Converter="{StaticResource SliderConverter}">
                        <Binding Path="Value" RelativeSource="{RelativeSource TemplatedParent}" />
                        <Binding Path="Maximum" RelativeSource="{RelativeSource TemplatedParent}" />
                        <Binding Path="ActualWidth" RelativeSource="{RelativeSource TemplatedParent}" />
                    </MultiBinding>
                </Rectangle.Width>
            </Rectangle>

            <!--  Трек с ползунком  -->
            <Track x:Name="PART_Track" Grid.Row="1">
                <Track.Thumb>
                    <Thumb Style="{StaticResource SliderThumbStyle}" />
                </Track.Thumb>
            </Track>
        </Grid>
    </ControlTemplate>

    <!--  Основной стиль для Slider  -->
    <Style x:Key="CustomSliderStyle" TargetType="{x:Type Slider}">

        <Setter Property="Height" Value="70" />
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Maximum" Value="2" />
        <Setter Property="IsSnapToTickEnabled" Value="True" />
        <Setter Property="TickFrequency" Value="1" />

        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Template" Value="{StaticResource HorizontalSlider}" />
            </Trigger>
        </Style.Triggers>
    </Style>


    <!--  Стиль для TextBox  -->

    <Style x:Key="EmailTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource ButtonBrush_transparent}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderBrush" Value="White" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="FontFamily" Value="SB Sans Text" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="Height" Value="74" />
        <Setter Property="Width" Value="796" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">

                    <Border
                        x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="37">
                        <Grid>
                            <!--  Обязательный элемент для текста  -->
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Margin="52,0"
                                VerticalAlignment="Center" />

                            <TextBlock
                                x:Name="HintText"
                                Margin="52,0"
                                VerticalAlignment="Center"
                                Foreground="{StaticResource ColorHintBrush}"
                                Text="{TemplateBinding Tag}"
                                Visibility="Collapsed" />
                        </Grid>
                    </Border>

                    <!--  Триггеры для интерактивности  -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="#FF80C0FF" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="#FF0080FF" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Text" Value="" />
                                <Condition Property="IsKeyboardFocused" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="HintText" Property="Visibility" Value="Visible" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>




    <Style x:Key="CustomCheckboxStyle" TargetType="{x:Type CheckBox}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <BulletDecorator Background="Transparent">
                        <BulletDecorator.Bullet>
                            <Grid Width="32" Height="32">
                                <Border
                                    x:Name="Border"
                                    Background="Transparent"
                                    BorderBrush="White"
                                    BorderThickness="1"
                                    CornerRadius="4" />


                                <Path
                                    x:Name="CheckMark"
                                    Width="14"
                                    Height="10"
                                    Data="M 0,10 L 5,15 L 15,5 L 17,7 L 5,19 L 0,14 Z"
                                    Fill="{StaticResource ButtonBrush_Green}"
                                    Opacity="0"
                                    Stretch="Uniform" />
                            </Grid>
                        </BulletDecorator.Bullet>
                        <ContentPresenter
                            x:Name="ContentPresenter"
                            Margin="10,0,0,0"
                            VerticalAlignment="Center" />
                    </BulletDecorator>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="White" />
                            <Setter TargetName="Border" Property="BorderBrush" Value="White" />
                            <Setter TargetName="CheckMark" Property="Opacity" Value="1" />

                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Opacity" Value="0.5" />
                            <Setter TargetName="ContentPresenter" Property="Opacity" Value="0.5" />
                            <Setter TargetName="CheckMark" Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BasePopupStyle" TargetType="Popup">
        <Setter Property="Placement" Value="Center" />
        <Setter Property="StaysOpen" Value="False" />
        <Setter Property="AllowsTransparency" Value="True" />
    </Style>


</ResourceDictionary>
